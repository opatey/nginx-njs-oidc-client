version: '3'
services:
  nginx:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./out:/etc/nginx/njs-modules:ro
    ports:
    - 8080:80
    environment:
    - NGINX_PORT=80

